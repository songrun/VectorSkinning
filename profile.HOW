python -m cProfile -o profile.out web-gui.py
python -c "import sys, pstats; pstats.Stats(sys.argv[1]).strip_dirs().sort_stats('cumulative').print_stats()" profile.out

import pstats
p = pstats.Stats('profile.out')
p = p.strip_dirs()
p.sort_stats('cumulative').print_stats(20)
# p.print_callers(.5, '__setitem__')
