for TEST in "simple_closed" "pebble" "alligator"
do
    python sparse_solver_speed_test.py "${TEST}" 2>&1 | tee "profiles/sparse_solver_speed_test.py.${TEST}.out"
done

for TEST in "simple_closed" "pebble" "alligator"
do
    egrep '=======>|died' "profiles/sparse_solver_speed_test.py.${TEST}.out"
done
